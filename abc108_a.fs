abc108_a.fs
##############################################
##############################################
##############################################
##############################################
stdin.ReadLine()
|> int
|> fun x -> if x % 2 = 0 then (x / 2) * (x / 2) else ((x + 1) / 2) * ((x - 1) / 2)
|> printfn "%d"
##############################################
stdin.ReadLine() |> int |> (fun n -> (n + n % 2) / 2 * (n / 2)) |> printfn "%d"
##############################################
open System

let inputArray = stdin.ReadLine().Split(' ') |> Array.map int
let n: int = inputArray.[0]
let ans: int = (n/2)*((n+1)/2)
ans |> printfn "%d"

##############################################
[python]
N=int(input())
print((N//2)*((N+1)//2))
##############################################
